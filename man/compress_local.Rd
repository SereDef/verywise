% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fed_helpers.R
\name{compress_local}
\alias{compress_local}
\title{Compress a site's results into a tar.gz archive}
\usage{
compress_local(outp_dir, site_name, then_rm_files = TRUE)
}
\arguments{
\item{outp_dir}{Character scalar. Directory containing the local output files
to be packaged (e.g., FBM \code{.rds} + \code{.bk} pairs and other \code{.rds} objects).}

\item{site_name}{Character scalar. Site identifier used as the archive basename
(output file will be \verb{\{site_name\}.tar.gz}).}

\item{then_rm_files}{Logical. Remove input files after compressing them?
Default: TRUE.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of creating the
\verb{\{site_name\}.tar.gz} file.
}
\description{
Creates a gzip-compressed tar archive containing a sufficient statistics
from the local site. The archive is written to the output directory and
can be safely shared among insititutions
}
\examples{
\dontrun{
# Suppose outp_dir contains:
#   XtY.rds, XtY.bk, psumsY.rds, psums.bk, static.rds
compress_local(outp_dir = "path/to/site_outputs", site_name = "siteA")
# Creates "siteA.tar.gz" in the current working directory.
}

}
\author{
Serena Defina, 2026.
}
