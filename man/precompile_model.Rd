% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lmm_helpers.R
\name{precompile_model}
\alias{precompile_model}
\title{Pre-compile a linear mixed model template}
\usage{
precompile_model(
  formula,
  data_list,
  tmp_y,
  measure,
  weights = NULL,
  REML = TRUE,
  lmm_control = lme4::lmerControl(calc.derivs = FALSE),
  verbose
)
}
\arguments{
\item{formula}{A two-sided \code{formula} object passed to \code{\link[lme4:lmer]{lme4::lmer()}}.
The left-hand side is replaced by a dummy outcome column named
\verb{vw_<measure>} before fitting.}

\item{data_list}{A \code{list} of \code{data.frame} objects, typically constructed
by \code{\link[=imp2list]{imp2list()}} Each element must contain all variables referenced by
\code{formula}.}

\item{tmp_y}{A numeric vector used as the dummy outcome. Must have the
same length as the number of rows in each element of \code{data_list}.}

\item{measure}{A character string naming the brain surface measure
(e.g. \code{"thickness"}, \code{"area"}). Used to construct the temporary
outcome column \verb{vw_<measure>} injected into each imputed dataset.}

\item{weights}{Either \code{NULL} (no observation weights), a numeric vector
of weights of the same length as the rows in each imputed dataset, or
a character string giving the name of a column in each dataset that
contains the weights.}

\item{REML}{Logical. Whether to use restricted maximum likelihood
estimation. Passed directly to \code{\link[lme4:lmer]{lme4::lmer()}}. Defaults to \code{TRUE}.}

\item{lmm_control}{A \code{lmerControl} object produced by
\code{\link[lme4:lmerControl]{lme4::lmerControl()}}, used to fine-tune the optimiser behaviour.
Defaults to \code{lme4::lmerControl(calc.derivs = FALSE)}.}

\item{verbose}{Logical. If \code{TRUE}, emits a \code{cli} progress step during
model construction.}
}
\value{
A \code{list} of the same length as \code{data_list}, where each element
is a fitted \code{lmerMod} object estimated on the corresponding imputed
dataset with the dummy outcome. These objects are intended for use
with \code{\link[lme4:refit]{lme4::refit()}} or equivalent downstream functions in the
\code{verywise} pipeline.
}
\description{
Fits a \code{lme4::lmer} model (on each imputed dataset) using a dummy outcome
variable. It produces a list of fitted model templates that can be updated
with vertex outcome data, avoiding the overhead of re-specifying the
random-effects structure for every vertex/surface measure.
}
\author{
Serena Defina, 2026
}
