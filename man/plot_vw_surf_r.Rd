% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vw_surf_r.R
\name{plot_vw_surf_r}
\alias{plot_vw_surf_r}
\title{Plot vertex-wise maps on brain surfaces}
\usage{
plot_vw_surf_r(
  lh = NULL,
  rh = NULL,
  surface = c("inflated", "pial", "white"),
  cmap = grDevices::colorRampPalette(c("blue", "white", "red")),
  bg_map = c("sulc", "curv", "none"),
  symm = TRUE,
  views = "t4",
  colorbar_legend = NULL,
  output_file = NULL,
  fs_home = Sys.getenv("FREESURFER_HOME"),
  fs_template = "fsaverage"
)
}
\arguments{
\item{lh, rh}{Numeric vector \strong{or} path to a \code{.mgh}/\code{.gii}
file, or \code{NULL}. Vertex-wise values for the left / right hemisphere.
At least one must be non-\code{NULL}.}

\item{surface}{\code{"inflated"} (default), \code{"pial"}, or
\code{"white"}.}

\item{cmap}{A \code{colorRampPalette}-style function.
Default: blue–white–red diverging palette.}

\item{bg_map}{Background shading: \code{"sulc"} (default),
\code{"curv"}, or \code{"none"}.}

\item{symm}{Logical. Use a symmetric (zero-centred) colour scale?
Default \code{TRUE}.}

\item{views}{Camera angles. \code{"t4"} (4 standard views, default),
\code{"t9"} (9 views), \code{"si"} (superior/inferior), or a character
vector of individual angle names.}

\item{colorbar_legend}{Character label for the colour bar, or \code{NULL}.}

\item{output_file}{Path ending in \code{.png} for static export, or
\code{NULL} for interactive mode.}

\item{fs_home}{FreeSurfer installation root. Defaults to
\env{FREESURFER_HOME}.}

\item{fs_template}{fsaverage template name. Default \code{"fsaverage"}.}
}
\value{
In interactive mode, an \code{rglwidget} object (auto-prints as a
WebGL scene). In static mode, the \code{output_file} path invisibly.
}
\description{
Render a vertex-wise scalar map on an fsaverage template surface using
\pkg{fsbrain}. Works cross-platform without XQuartz:
\itemize{
\item \strong{Interactive} (no \code{output_file}): returns an
\code{\link[rgl]{rglwidget}} that opens in the RStudio Viewer,
Positron, or any browser. No display server required.
\item \strong{Static PNG} (\code{output_file} supplied): renders via
\code{webshot2}/headless-Chrome on macOS and other platforms
without native OpenGL snapshot support; falls back to direct
OpenGL snapshot on Linux with a display.
}
}
