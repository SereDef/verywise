# Generate a set of backing file (bk) names

Generate a set of backing file (bk) names

## Usage

``` r
build_output_fbm(
  result_path,
  type,
  nrow,
  ncol,
  precision = "float",
  verbose = FALSE
)
```
