# Print decorated message to console if verbose = TRUE

Print decorated message to console if verbose = TRUE

## Usage

``` r
vw_pretty_message(msg, ..., verbose = TRUE)
```

## Arguments

- msg:

  Content to print

- ...:

  Other arguments to pass to prettify_message

- verbose:

  (default = TRUE)
