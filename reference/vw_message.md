# Print message to console if verbose = TRUE

Print message to console if verbose = TRUE

## Usage

``` r
vw_message(..., verbose = TRUE)
```

## Arguments

- ...:

  : content to print

- verbose:

  (default = TRUE)
